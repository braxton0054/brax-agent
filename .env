# BRAX — API Keys
# Set keys here OR during onboarding. Keys set during onboarding take priority.
# To use a provider, uncomment and fill in its API key.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Default provider settings (overridden by per-agent config during onboard)
BRAX_DEFAULT_PROVIDER=anthropic
BRAX_DEFAULT_MODEL=claude-sonnet-4-20250514
